program factorial
{
# declarations #
    declare x;
    declare i,fact;
    # main #
    if (x<i and x>i)
    {
        x := 2;
    };
    input(x);
    fact:=1;
    i:=1;
    while (i<=x)
    {
        fact:=fact*i;
        i:=i+1;
    };
    print(fact);
}.